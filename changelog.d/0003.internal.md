- Fully namespace internal references: internal modules now import from their defining submodules (no dependency on package/model aggregator `__init__` re-exports), eliminating ambiguity and future naming conflicts.
- Add an AST-based pytest guard (`test_no_module_level_functions.py`) that fails on any module-level `def` / `async def` / `lambda` assignment, enforcing the no-standalone-functions rule.
- Document the namespacing rules (`.claude/rules/namespacing.md`), including the `emit()` first-argument tag convention for future APIs.
- Add SQL Server sync/async test providers (`sqlserver_sync.py` / `sqlserver_async.py`) and register them, extending the multi-backend test matrix.
- Add Oracle sync/async test providers (`oracle_sync.py` / `oracle_async.py`) and register them, extending the multi-backend test matrix.
- Add Firebird sync/async test providers (`firebird_sync.py` / `firebird_async.py`) and register them, extending the multi-backend test matrix.
- Make `Schema.drop_tables` dialect-aware: Firebird (no `DROP TABLE IF EXISTS`) gets an EXECUTE BLOCK existence guard; other dialects keep the plain form.
- Make `Schema._table_expressions` timestamp columns dialect-aware: Oracle and Firebird use a real TIMESTAMP (TEXT/CLOB columns cannot store or compare datetimes there); other backends keep TEXT ISO strings.
