- Add `AiAgentAssistant` pre-built application demonstrating a **runtime-defined execution graph**: a seed `plan` step is expanded into a tool-call chain (`search` → `write` → `review`) via the new transactional `SyncOrderService.append_subprocess` / `AsyncOrderService.append_subprocess` API, with reverse-order compensation on failure.
- Add `OrderEvent.subprocess_appended` / `AsyncOrderEvent.subprocess_appended` factory methods, activating the previously-defined `stateflow:event:sp_appended` event type.
- Add async query helper siblings (`AsyncOrderQuery`, `AsyncOrderSubProcessQuery`, etc.) for all 9 models, completing sync/async parity at the query layer.
