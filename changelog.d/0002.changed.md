- Namespace all framework-reserved string tags to prevent collisions with user-declared business states or other frameworks: event types (`stateflow:event:*`), outbox topics (`stateflow:topic:*`), order/template/subprocess/rollback/outbox statuses and subprocess sources. User-declared states (`terminal_states`, etc.) are intentionally unaffected.
- Persisted values changed; this is a breaking change acceptable during the pre-release phase.
